# GigaBuddy — Roadmap / Дорожная карта проекта

> Единый чеклист всех задач проекта GigaBuddy (не только текущего релиза).
> Ставим `[x]` по мере продвижения. Сверять с live runtime и git-историей, не с памятью.
>
> **Проект:** SnakeWatch хакатон — GigaBuddy (Ouroboros Onboarding Mentor).
> **Репозиторий:** приватный `ilysha95ab-afk/gigabuddy`, ветка `ouroboros`.
> **Последнее обновление:** 2026-07-19 · v6.83.0 (ОБРАТИМАЯ САМОЭВОЛЮЦИЯ ГигаБадди за реальным immune-гейтом: `propose/approve/revert_evolution` в `gigabuddy_state` + чистый `gigabuddy_evolution.py`; глубины `interface`/`role_tempo`/`ui`, DECLARED enum (не text-классификация, BIBLE P5), обратимость через `pre_image`+git-теги, novice-safe view не отдаёт proposals/pre_image/git_tag_hint. triad+scope на cloud.ru реальны, advisory авто-байпас без ANTHROPIC_API_KEY. Форма Б, дерево грязное для ручного коммита владельца). Ранее v6.82.0 (C/#4 ПЕРЕРАБОТАН в НАТИВНЫЙ LLM-конвейер: наставник кладёт СЫРЫЕ текстовые файлы `.txt`/`.md`/`.docx` в `employees/<id>/knowledge/` БЕЗ ручной разметки — Ouroboros сам строит вики. `_extract_text` (docx через опциональный fail-soft `docx2txt`, `.txt`/`.md` всегда), на BUILD-этапе (не на hot-path) `_llm_build_chunks` через LIGHT LLM (LLMClient+usage_accounting) режет на семантические куски и ГЕНЕРИРУЕТ теги+`[[связи]]`; fail-soft деградация на детерминированный chunking при недоступности LLM, без выдумывания. Хранение в `knowledge/.wiki_index/index.json`, автообновление по mtime→фоновый rebuild из gateway. Индикатор статуса в правой панели (`knowledgeBase:{status,docCount,chunkCount,llmBuilt}`, view-pure). Retrieval BM25+граф прежний, НЕ RAG. Форма Б, дерево грязное для ручного коммита владельца). Ранее v6.81.0 (первая версия «вики Карпатого» с ручной разметкой), v6.80.1 (`/clean` + граница персоны), v6.80.0 (C/#5: методология). **Осталось (C):** Telegram-протокол наставника; demo accelerator; презентация.

---

## ✅ Сделано (реализовано и в проде)

- [x] **Safety Supervisor fix** — `ouroboros/llm.py`, распознавание cloud.ru value-rejection (`response_format.type: Input should be 'json_schema'`) → one-shot strip-and-retry.
  _v6.72.0, commit `b335c78`_
- [x] **Git/GitHub путь** — приватный репо `ilysha95ab-afk/gigabuddy`, ветка `ouroboros` запушена и проверена (`git ls-remote`).
- [x] **Vision model настроен** — OpenRouter `gpt-4o`, проверен реальным анализом скриншота.
- [x] **GigaBuddy product-mode shell** — бренд «ГигаБадди», subtitle «персональный ИИ-наставник адаптации», боковая панель адаптационного трека (Советчик → Помощник → Партнёр), скрытие novice-facing developer chrome, кнопка Panic сохранена.
  _v6.71.1, commit `d6b6869`_
- [x] **PIN-return escape hatch** — выход из product mode через 4-значный `GIGABUDDY_ADMIN_PIN`: `_handle_gigabuddy_return_action` + `_generic_gigabuddy_settings_guard`, `POST /api/settings` `_action=gigabuddy_return` (проверка PIN только на сервере).
  _v6.71.2, commit `22b41f9`_
- [x] **Mutable GigaBuddy state reducer** — `ouroboros/gigabuddy_state.py`: синтетические профили сотрудников (alice-demo / leonid-demo / blank-demo), стадии наставничества advisor → assistant → partner, задачи, пакеты опросника, novice-safe view projection. Действия: `get_state` / `select_employee` / `add_task` / `approve_stage` / `reject_stage` / `rollback` / `demo_accelerate`. Mentor/admin-кнопки убраны из UI новичка (наставник взаимодействует через Telegram / PIN-return). 35 тестов.
  _v6.73.0, commit `4094f55`_
- [x] **Устойчивое состояние GigaBuddy + interface-атрибуты (пункт 1 плана).** Конфиг сотрудника расширен до полного состояния: `profile` (имя/роль/отдел/опыт/интересы), стадия, адаптационный `track` (этапы advisor → assistant → partner со статусами), прогресс (выводится из трека), версия поведения, `rollback_history`, mentor notes — ПЛЮС блок `interface` для гиперперсонификации (тема, акцентный цвет в рамках дизайн-системы, маскот, тон, layout). Правая панель (`web/modules/gigabuddy.js`) читает живое состояние вместо статики; интерфейсные атрибуты применяются к product-mode оболочке через существующий settings-seam (`data-gigabuddy-theme/tone` + CSS-переменная `--gigabuddy-accent`), без frozen-роутов и правки `contracts.py`. Границы соблюдены: чувствительные `internal_signals`, mentor notes и rollback history не попадают в novice-view; значения валидируются (allow-list тем/тонов, hex-only акцент). Тесты и docs обновлены.
  _v6.74.0_
- [x] **B1: изолированный чистый чат новичка + трёхколоночная раскладка.** Product-mode оболочка стала цельной 3-колоночной раскладкой (скелет трека СЛЕВА / чистый чат новичка В ЦЕНТРЕ / панель СПРАВА), без project-room-оверлея; тред новичка (`gigabuddy-novice`) реально партиционируется на бэкенде через существующий project/chat_id роутинг (`ensure_novice_project`, эагер-регистрация в `/api/state`). Это UI/history партиционирование — фокусная комната в едином сознании (BIBLE P1), не изоляция памяти.
  _v6.76.0_
- [x] **B1: роль-контракт / персона ГигаБадди для треда новичка.** В product mode и ТОЛЬКО для треда новичка в системный контекст инжектится персона наставника (`ouroboros/gigabuddy_state.py::gigabuddy_persona_section` → `build_gigabuddy_persona`, в `context.py::_capture_context_core`): жёсткая граница (не говорить про Ouroboros/архитектуру/версии/эволюцию/разработку, мягко возвращать в роль), персонализация из persistent-состояния (имя/роль/отдел/опыт/интересы + тон interface), знание текущей стадии (Советчик→Помощник→Партнёр, статус справа) и адаптационного трека, точка интеграции с папкой базы знаний сотрудника (`~/Ouroboros/gigabuddy/employees/<id>/knowledge/`, без выдумывания фактов — retrieval это B3/C). Двойной гейт (product mode + `project_id==gigabuddy-novice`): обычный Ouroboros и чат разработчика не изменены (пинится тестом-инвариантом). Read-only над novice-safe view (чувствительное не утекает), fail-soft. Без правки frozen-контрактов/BIBLE.
  _v6.77.0_
- [x] **B2: анимированный адаптационный трек СЛЕВА.** Левый слот `data-gigabuddy-track-slot` рендерит живой адаптационный трек СОТРУДНИКА (этапы/шаги из `view.track`), а не роли Советчик→Помощник→Партнёр (статус роли остаётся справа). Каждый этап — раскрывающийся `<details>` («проваливаться» в этап и видеть его шаги); активный этап открыт по умолчанию, бейдж done/N показывает прогресс, акцент через `--gigabuddy-accent`. Рендерится `refreshGigaBuddyPanel` (единственный владелец get_state) из того же view. View-проекция получила опциональное поле `steps[]` на этап (view-pure, пусто по умолчанию, bounded, не выдумывается — опросник это B3/C); пустой этап показывает нейтральную подсказку, пустой трек — нейтральный плейсхолдер. Только CSS-переменные, glassmorphism, плавные transitions; трёхколоночность деградирует в одну колонку на узком экране; Panic+PIN и novice-границы не регрессировали; frozen-контракты/BIBLE не тронуты.
  _v6.78.0_
- [x] **B3: чтение сотрудника из файлов — нейтральный старт + парсинг профиля + чат-опросник.** Свежий старт стал НЕЙТРАЛЬНЫМ и безымянным (`_blank_employee`, id `novice`): синтетическая Алиса убрана и из дефолта reducer'а, и из frontend-фолбэка; панели показывают честные плейсхолдеры до загрузки профиля. Alice/Leonid — ЗАГРУЖАЕМЫЕ демо-конфиги (`load_profile`), не хардкод-дефолт. Новый fail-soft, folder-confined `ouroboros/gigabuddy_profile.py` парсит профиль из `~/Ouroboros/gigabuddy/employees/<id>/` (JSON или markdown-frontmatter), точки интеграции `has_questionnaire`/`knowledge_dir_exists` под #4/#5. Три durable-опа (`load_profile`/`set_track`/`record_progress`): чат-опросник (персона) приветствует по имени, ведёт 30-60-90-знакомство и пишет реальные `track.steps` (которые рендерит B2), прогресс — в durable per-employee state, не только в диалоге. Единое сознание (BIBLE P1), novice-безопасность, Panic/PIN, frozen-контракты/BIBLE не тронуты. Дисциплина «не выдумывать трек» теперь для всех сотрудников.
  _v6.79.0_

---

## ⬜ Не сделано (осталось)

- [x] **Диагностическое интервью → построение персонального адаптационного трека.** GigaBuddy проводит опрос новичка (чат-сценарий персоны) и строит индивидуальный трек онбординга (`set_track` наполняет `steps[]`; durable-прогресс через `record_progress`). Чувствительные выводы (напр. тревожность) — только внутренние, новичку не показываются (novice-safe view). _v6.79.0._ Методология вынесена во встроенный сценарий; отдельный скилл методологии (#5) — точка интеграции, ещё не построен.
- [x] **Скилл «вики Карпатого» (#4) — НАТИВНЫЙ LLM-конвейер (v6.82.0).** Переработан из первой версии (v6.81.0, требовала ручной markdown-разметки) в нативный конвейер: наставник кладёт СЫРЫЕ `.txt`/`.md`/`.docx` БЕЗ тегов/`[[ссылок]]`/заголовков — Ouroboros сам строит вику. `_extract_text`: `.txt`/`.md` читаются напрямую (byte-capped), `.docx` через опциональный fail-soft `docx2txt` (объявлен в `pyproject.toml`; отсутствует → честный skip .docx, `.txt`/`.md` всегда работают). На BUILD-этапе (НИКОГДА не на per-turn hot-path) `_llm_build_chunks` шлёт чистый текст в LIGHT LLM-слот через `LLMClient`+`usage_accounting` (`_build_wiki_prompt` через `.replace()`, не `.format()` — иначе `KeyError` на литеральных `{}` в JSON-примере), модель режет на семантические куски и ГЕНЕРИРУЕТ теги+cross-topic `[[связи]]` (методика Карпатого). Fail-soft: при недоступности/ошибке LLM файл деградирует на детерминированный `_chunk_markdown`, индекс помечается `llm_built=False`, факты не выдумываются. Хранение построенной вики в `knowledge/.wiki_index/index.json` (`save_index`/`load_persisted_index`, atomic, folder-confined, schema-versioned); удаление файла → куски уходят на ребилде, удаление `.wiki_index/` → пересобирается из исходников; автообновление по изменению состава/mtime (`_folder_signature` исключает service-dir) → `knowledge_status`=`building` → ОДНОРАЗОВЫЙ фоновый daemon-rebuild из gateway (`_maybe_trigger_knowledge_rebuild`, дедуп против polls, non-blocking). Retrieval (BM25+граф) прежний, НЕ RAG. Индикатор в правой панели: view-pure `knowledgeBase:{status,docCount,chunkCount,llmBuilt}` (`_knowledge_base_view`→`knowledge_status`, без триггера LLM) → `renderKnowledgeStatus`/`normalizeKnowledgeBase` (`web/modules/gigabuddy.js`, CSS `.gigabuddy-knowledge-status`, только CSS-переменные): «база обновляется…» / «база обновлена»+число доков/кусков / honest empty/error. Персона читает только ПЕРСИСТЕНТНЫЙ индекс (`get_index` `use_llm=False` на hot-path). Без external skill/skill-review; двойной гейт персоны, novice-безопасность, Panic/PIN, frozen-контракты/`BIBLE.md` не тронуты, reducer view-pure, модуль в бюджете (1035<1600). _v6.82.0._
- [x] **Методология опросника / плана адаптации (#5).** Явная методология построения трека встроена в персону/ядро (форма Б, БЕЗ external skill/skill-review — на этой машине нет `ANTHROPIC_API_KEY`, настоящий скилл не стал бы executable, а методология тесно связана с персоной в ядре): `_METHODOLOGY_GUIDANCE` + `_methodology_block(...)` в `ouroboros/gigabuddy_state.py` инжектится в сценарий знакомства персоны. Опирается на две мировые практики — арка 30-60-90 дней и адаптация по компетенциям — творчески и гиперперсонально (тон/глубина под профиль, не сухой шаблон). Когда наставник положил базовый опросник в `employees/<id>/questionnaire/`, персона опирается на его реальные вопросы (`gigabuddy_profile.read_questionnaire_hints`, fail-soft, folder-confined, bounded); без опросника — дефолтный 30-60-90/competency-сценарий, без выдумывания. Чувствительные наблюдения — внутренние, новичку не показываются (novice-safe). _v6.80.0._
- [x] **Контекстные команды новичка + усиление границы персоны.** `/clean` — визуальная очистка чата новичка: перехват ВНУТРИ треда новичка на фронте (`web/modules/chat.js`, гейт по `gigabuddy-novice` `projectId`), НЕ рантайм-слэш (супервизор не трогается). Чистит только видимый диалог + in-memory/session-состояние треда, показывает ОДНО фиксированное нейтральное приветствие (эфемерное, не персистится). Durable-состояние адаптации (профиль/стадия/`track.steps`/прогресс) НЕ трогается — новичок не обнуляет трек; серверный `chat.jsonl` цел (per-session визуальная очистка, reload ре-синкает по дизайну). Плюс жёсткая граница персоны (`build_gigabuddy_persona`) дополнена явным запретом упоминать новичку режим/процессы разработчика (product mode, dev-режим, `/restart`, коммиты, версии). Форма Б, единое сознание (BIBLE P1), novice-безопасность и двойной гейт не регрессировали, Panic/PIN на месте, frozen-контракты/BIBLE не тронуты. _v6.80.1._
- [x] **Обратимая самоэволюция ГигаБадди (mentor-approved, depth-classified).** Запрос новичка ИЛИ решение самого ГигаБадди → durable, одобряемая наставником эволюция в per-employee `gigabuddy_state`, строго в мягком слое плана А: `interface` (тема/акцент/маскот/тон/layout — валидируется существующими валидаторами дизайн-системы, без произвольного CSS), `role_tempo` (переход стадии Советчик→Помощник→Партнёр), `ui` (proposal-only заметка, без мутации). Новый чистый модуль `ouroboros/gigabuddy_evolution.py` (валидаторы как callables → без import-cycle) строит/применяет/откатывает предложение; три опа (`propose_evolution`/`approve_evolution`/`revert_evolution`) зарегистрированы во ВСЕХ ТРЁХ реестрах с ДЕКЛАРИРОВАННЫМ enum глубины (reducer не text-классифицирует запрос — BIBLE P5). Обратимость: явный `pre_image` (interface/role_tempo сохраняют весь interface-блок + стадию + `active_behavior_version_id` + track → derived `progressPct` восстанавливается точно, не «v1») И git-теги (`git_tag_hint` `giga-evo-<id>-<n>`; каждый шаг = 1 коммит+annotated tag владельца, «сырой ГигаБадди» = чекаут тега, baseline v6.82.0). `propose` ничего не применяет; `approve` идемпотентен (double-approve → raise); предложения active-employee-scoped; unknown depth отклоняется. Immune-гейт настоящий: triad+scope на cloud.ru (advisory авто-байпасится без `ANTHROPIC_API_KEY` — non-blocking, EXPECTED). novice-safe view НИКОГДА не отдаёт `evolution_proposals`/`pre_image`/`git_tag_hint` (расширенный sentinel-тест); reducer view-pure; единое сознание (BIBLE P1); Panic/PIN на месте; frozen-контракты/`BIBLE.md` не тронуты. Telegram approve/reject — отложенный финальный транспорт. _v6.83.0._
- [ ] **Telegram mentor protocol.** Наставник общается с GigaBuddy через Telegram (одобрение переходов между стадиями, постановка задач, просмотр прогресса) — вне UI новичка.
- [x] **UI-персонализация под интересы новичка (основа).** Interface-атрибуты в конфиге сотрудника (тема/акцент/маскот/тон/layout) применяются к оболочке через settings-seam; пример: кошачья тема для Алисы. «Гиперперсональный агент как базовый минимум Сбера». _v6.74.0._ Дальше — диагностический вывод интересов из интервью (пункт ниже).
- [ ] **Demo accelerator: end-to-end сценарий < 3 минут.** Полный демо-прогон (онбординг → интервью → трек → эволюция стадии → результат) уложить в 3 минуты. **30 % оценки хакатона** — наивысший приоритет из оставшегося.
- [ ] **README / презентация проекта для хакатона SnakeWatch.** Описание продукта, архитектуры, демо-инструкция, скриншоты.

---

## Прогресс

**15 / 17 задач готово.** Ядро продукта (product-mode shell, безопасный выход, мутабельное состояние, изоляция чистого чата новичка + 3-колоночная раскладка, роль-контракт/персона наставника, анимированный трек слева, нейтральный старт + парсинг профиля из файлов + чат-опросник наполняет трек, методология построения трека #5 встроена в персону, вики Карпатого #4 — retrieval по базе знаний отдела, обратимая самоэволюция ГигаБадди за реальным immune-гейтом, инфраструктура: safety/git/vision) собрано. Осталось для демо: Telegram-протокол, demo accelerator и презентация.
