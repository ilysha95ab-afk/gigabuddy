{
  "name": "ouroboros-web",
  "version": "6.85.0",
  "private": true,
  "type": "module",
  "description": "Ouroboros browser UI package boundary",
  "scripts": {
    "lint": "node --check app.js",
    "test": "node --test tests/*.test.js"
  }
}
