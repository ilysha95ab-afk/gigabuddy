{
  "name": "ouroboros-web",
  "version": "6.88.1",
  "private": true,
  "type": "module",
  "description": "Ouroboros browser UI package boundary",
  "scripts": {
    "lint": "node --check app.js",
    "test": "node --test tests/*.test.js"
  }
}
